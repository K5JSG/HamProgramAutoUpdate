<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Palette carried over from the web dashboard -->
    <SolidColorBrush x:Key="BgBrush"        Color="#1A1A2E" />
    <SolidColorBrush x:Key="CardBrush"      Color="#16213E" />
    <SolidColorBrush x:Key="PanelBrush"     Color="#12182F" />
    <SolidColorBrush x:Key="BorderBrush2"   Color="#2A2A4A" />
    <SolidColorBrush x:Key="BorderHover"    Color="#3A3A5A" />
    <SolidColorBrush x:Key="TextBrush"      Color="#EAEAEA" />
    <SolidColorBrush x:Key="MutedBrush"     Color="#888888" />
    <SolidColorBrush x:Key="DimBrush"       Color="#666666" />
    <SolidColorBrush x:Key="AccentBrush"    Color="#00D4AA" />
    <SolidColorBrush x:Key="AccentHover"    Color="#00E8BB" />
    <SolidColorBrush x:Key="DangerBrush"    Color="#FF4757" />
    <SolidColorBrush x:Key="DangerLight"    Color="#FF6B7A" />
    <SolidColorBrush x:Key="WarnBrush"      Color="#FFC300" />

    <SolidColorBrush x:Key="AccentFill"     Color="#00D4AA" Opacity="0.12" />
    <SolidColorBrush x:Key="AccentBorder"   Color="#00D4AA" Opacity="0.5" />

    <FontFamily x:Key="MonoFont">Consolas, Cascadia Mono, Courier New</FontFamily>

    <!-- ==== Buttons ==== -->

    <Style x:Key="BaseButton" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="14,7" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.85" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.45" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
        <Setter Property="Background" Value="{StaticResource AccentBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
        <Setter Property="Foreground" Value="#1A1A2E" />
    </Style>

    <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
        <Setter Property="Background" Value="{StaticResource BorderBrush2}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush2}" />
    </Style>

    <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="#66FF4757" />
        <Setter Property="Foreground" Value="{StaticResource DangerLight}" />
    </Style>

    <Style x:Key="SmallButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
        <Setter Property="Padding" Value="12,5" />
        <Setter Property="FontSize" Value="11" />
    </Style>

    <Style x:Key="SmallPrimary" TargetType="Button" BasedOn="{StaticResource SmallButton}">
        <Setter Property="Background" Value="{StaticResource AccentBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
        <Setter Property="Foreground" Value="#1A1A2E" />
    </Style>

    <Style x:Key="SmallDanger" TargetType="Button" BasedOn="{StaticResource SmallButton}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="#66FF4757" />
        <Setter Property="Foreground" Value="{StaticResource DangerLight}" />
    </Style>

    <Style x:Key="SmallSecondary" TargetType="Button" BasedOn="{StaticResource SmallButton}">
        <Setter Property="Background" Value="{StaticResource BorderBrush2}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush2}" />
    </Style>

    <!-- ==== Text ==== -->

    <Style x:Key="LabelText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="10" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
        <Setter Property="Margin" Value="0,0,0,3" />
    </Style>

    <Style x:Key="ValueText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource MonoFont}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

</ResourceDictionary>
